{
  "name": "github-copilot-chat-usage",
  "publisher": "ailmind",
  "displayName": "GitHub Copilot Chat Usage",
  "description": "Analyze token usage and costs from GitHub Copilot Chat debug logs",
  "version": "0.6.0",
  "license": "MIT",
  "icon": "images/ailmind-icon-128.png",
  "homepage": "https://ailmind.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/ailmind/github-copilot-chat-usage.git"
  },
  "galleryBanner": {
    "color": "#1F2937",
    "theme": "dark"
  },
  "engines": {
    "vscode": "^1.101.0"
  },
  "categories": [
    "Visualization",
    "Other"
  ],
  "keywords": [
    "copilot",
    "usage",
    "ai credits",
    "tokens",
    "costs"
  ],
  "activationEvents": [
    "onCommand:copilotUsageTracker.analyzeSession",
    "onCommand:copilotUsageTracker.pickSession",
    "onCommand:copilotUsageTracker.refresh",
    "onView:copilotUsageTree",
    "onChatParticipant:copilot-usage-tracker.usage",
    "onLanguageModelTool:usage-search-sessions",
    "onLanguageModelTool:usage-get-graph"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "copilotUsageTracker.analyzeSession",
        "title": "Copilot Usage: Analyze Current Session"
      },
      {
        "command": "copilotUsageTracker.pickSession",
        "title": "Copilot Usage: Pick Session",
        "icon": "$(search)"
      },
      {
        "command": "copilotUsageTracker.pickSession.loading",
        "title": "Loading Sessions...",
        "icon": "$(sync~spin)"
      },
      {
        "command": "copilotUsageTracker.refresh",
        "title": "Refresh",
        "icon": "$(refresh)"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "copilot-usage",
          "title": "Copilot Usage",
          "icon": "$(dashboard)"
        }
      ]
    },
    "views": {
      "copilot-usage": [
        {
          "id": "copilotUsageTree",
          "name": "Token Usage"
        }
      ]
    },
    "menus": {
      "view/title": [
        {
          "command": "copilotUsageTracker.pickSession",
          "when": "view == copilotUsageTree && !copilotUsageTracker.isPickingSession",
          "group": "navigation"
        },
        {
          "command": "copilotUsageTracker.pickSession.loading",
          "when": "view == copilotUsageTree && copilotUsageTracker.isPickingSession",
          "group": "navigation"
        },
        {
          "command": "copilotUsageTracker.refresh",
          "when": "view == copilotUsageTree",
          "group": "navigation"
        }
      ]
    },
    "viewsWelcome": [
      {
        "view": "copilotUsageTree",
        "contents": "No Copilot Chat session data found yet.\n\n**Option 1 — File logging (debug detail):**\nSet `github.copilot.chat.agentDebugLog.fileLogging.enabled` to **true**, then start a new Copilot Chat session.\n\n[Open Setting](command:workbench.action.openSettings?%22github.copilot.chat.agentDebugLog.fileLogging.enabled%22)\n\n**Option 2 — SQLite OTel data (exact tokens):**\nEnable `copilotUsageTracker.useSqliteData` to read token usage from the Copilot OTel database. This also enables the OTel span exporter automatically.\n\n[Open Setting](command:workbench.action.openSettings?%22copilotUsageTracker.useSqliteData%22)",
        "when": "!copilotUsageTracker.hasSession"
      }
    ],
    "chatParticipants": [
      {
        "id": "copilot-usage-tracker.usage",
        "fullName": "Usage Analyzer",
        "name": "usage",
        "description": "Analyze Copilot chat session data — costs, tool calls, commands, patterns. Ask about any session.",
        "isSticky": false
      }
    ],
    "languageModelTools": [
      {
        "name": "usage-search-sessions",
        "tags": [
          "copilot",
          "usage",
          "sessions"
        ],
        "toolReferenceName": "searchSessions",
        "displayName": "Search Chat Sessions",
        "modelDescription": "Search recent Copilot Chat sessions by keyword (supports multiple words as OR match). Always returns the last 2 days of sessions as a safety net alongside search results. Use this FIRST before usage-get-graph unless the user explicitly says 'this session' or 'the current one'. Returns session IDs you can pass to usage-get-graph.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "description": "Keywords to match against session titles (space-separated, OR match — any word matching counts). Like a wildcard search."
            },
            "daysBack": {
              "type": "number",
              "description": "How many days back to search (default: 3)"
            },
            "limit": {
              "type": "number",
              "description": "Maximum sessions to return."
            }
          }
        }
      },
      {
        "name": "usage-get-graph",
        "tags": [
          "copilot",
          "usage",
          "analysis"
        ],
        "toolReferenceName": "getGraph",
        "displayName": "Get Session Graph",
        "modelDescription": "Get the full data graph for a Copilot Chat session. Returns stats, messages, tool calls, nested subagent messages/turns, commands, and costs. Omit sessionId only when the user says 'this session' or 'the loaded one'. Otherwise call usage-search-sessions first to get the correct session ID. Analyze the returned data for risks, patterns, waste, repeated terminal commands, and tool usage.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "sessionId": {
              "type": "string",
              "description": "Session ID (or prefix) to load. Omit for the current session."
            },
            "includeMessages": {
              "type": "boolean",
              "description": "Whether to include per-message summaries."
            },
            "maxMessages": {
              "type": "number",
              "description": "Maximum messages to include."
            }
          }
        }
      }
    ],
    "configuration": {
      "title": "Copilot Usage Tracker",
      "properties": {
        "copilotUsageTracker.useSqliteData": {
          "type": "boolean",
          "default": false,
          "description": "Use Copilot SQLite Data: when enabled, reads token usage from the Copilot OTel SQLite database (`agent-traces.db`) instead of the chat-session JSONL files. Enabling this also sets `github.copilot.chat.otel.dbSpanExporter.enabled` to true so the database is populated."
        },
        "copilotUsageTracker.searchRoots": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "Additional directories to scan for Copilot/VS Code chat debug logs. Use absolute paths."
        },
        "copilotUsageTracker.maxSearchDepth": {
          "type": "number",
          "default": 6,
          "minimum": 1,
          "maximum": 12,
          "description": "Maximum recursion depth when scanning configured and default log roots."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc --watch -p ./",
    "test": "npm run compile && node test/parser.test.js",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^20.14.10",
    "@types/vscode": "^1.101.0",
    "@vscode/vsce": "^3.0.0",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1"
  }
}
